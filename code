void setup() {
size(300, 300);
background(255);
}
void draw() {
  float diam = random(30);
  
  noStroke();
  fill(random(255), random(255), random(255), random(255));
  ellipse(random(width), random(height), diam, diam);
}
